# AUTOLEARNING LOG — cross-agent self-improvement ledger
Standing law (Jesus, 2026-08-09): every completed transmission/distribution cycle appends its
lessons here, and each agent brain (Claude memory, Codex memories, Genesis cloud memory) absorbs
the entry. Append-only. Format: date | cycle | lesson | who learned it.

2026-08-09 | T33 release | Model routing law for Codex work: transmissions = Sol/medium reasoning (~268k tokens); mechanical packets/follow-ups = Terra/low (~108k tokens, ~3x faster, same quality on structured work). Route by task class, not habit. | Claude+Codex
2026-08-09 | T33 release | codex exec hygiene: always close stdin (`< /dev/null`), never escape quotes around output redirects, run long briefs in background to a file. Default xhigh reasoning silently exceeds 10-min timeouts. | Claude
2026-08-09 | T33 release | Codex's sandbox cannot launch Chrome headless — PNG rendering belongs to Claude outside the sandbox (render-carousel.sh works first try). Division of labor: Codex authors source, Claude renders + verifies. | Claude+Codex
2026-08-09 | T33 release | Privacy-as-test pattern PROVEN: a repo test that fails the build if the pre-public persona's name appears on any public surface. Laws enforced by tests survive careless edits; laws in prose do not. Reuse for every future boundary. | Codex
2026-08-09 | T33 release | Countersign flow worked end-to-end: Codex builds through its governed skill, Claude independently re-verifies (privacy scan, fact check, HTTP smoke, source-of-truth gate), deploy only after both gates. No new authorization was ever needed between agents — the human's task order suffices. | Claude+Codex
2026-08-09 | T33 social | Social packets are opt-in commissioned, never automatic with a release. Delivery lane until the dedicated Telegram connection exists: save locally (repo + Downloads ZIP) -> ship slides to cloud workspace -> Genesis delivers to Jesus via Telegram -> Jesus posts manually. | all brains
