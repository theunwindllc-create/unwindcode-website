# UnwindCode.ai Website Audit

Last updated: 2026-06-06

## Backend/API Findings

- `api/subscribe.js` is the active same-origin Vercel endpoint for homepage email capture.
- The endpoint normalizes email addresses, rejects malformed input before storage, and writes to Supabase REST when `SUPABASE_SERVICE_ROLE_KEY` is configured.
- If service-role storage is not configured, the endpoint falls back to the existing Supabase Edge Function path.
- Public failure responses now avoid returning raw upstream storage details, reducing the risk of exposing provider errors, schema details, or secret-adjacent text to browsers.
- `POST /api/subscribe` now rejects browser requests from untrusted cross-site origins before storage. Production origins are allowed by default; preview/custom origins can be added with `SUBSCRIBE_ALLOWED_ORIGINS`.
- Subscribe origin validation no longer trusts reconstructed `Host` / `X-Forwarded-Proto` origins. Missing-origin requests with unreviewed host headers are rejected, and localhost origins are development-only unless explicitly configured in `SUBSCRIBE_ALLOWED_ORIGINS`.
- Optional `page_path` metadata is accepted only as a same-site path, stripped to its pathname, and rejected before storage when it looks like an external URL, protocol-relative URL, overlong value, non-string value, or control-character payload.
- `SUBSCRIBE_LOG_EVENTS=true` enables metadata-only operational logs for accepted, rejected, and failed subscriber attempts without logging email addresses, service keys, or upstream provider details.
- When `RATE_LIMIT_REST_URL`, `RATE_LIMIT_REST_TOKEN`, and `RATE_LIMIT_SALT` are configured, `POST /api/subscribe` performs a durable pre-storage rate-limit check using a salted hash key, not the raw client address or email.
- Durable subscribe rate-limit identity now hashes route plus a validated client address only. Forwarded client headers are ignored by default, `User-Agent` is not part of the primary quota bucket, and `RATE_LIMIT_TRUST_PROXY_HEADERS=true` is required before `cf-connecting-ip`, `x-real-ip`, or the first `x-forwarded-for` value can influence the key.
- In `VERCEL_ENV=production`, `POST /api/subscribe` fails closed with a generic `503` when durable rate-limit configuration is missing, the limiter is unavailable, or the configured limiter returns a non-2xx HTTP error, preventing storage from bypassing the abuse gate.
- In `VERCEL_ENV=production`, `POST /api/subscribe` now requires explicit upstream configuration before storage. Supabase URL must be configured, and either the service-role REST key or anon Edge Function key must be configured; missing upstream env returns a generic `503` after the durable limiter check and before any Supabase request.
- Successful `POST /api/subscribe` responses now return only operational status and destination metadata; they no longer echo the normalized subscriber email back to the browser.
- The browser now calls only same-origin `/api/subscribe` for homepage signup. Direct provider fallback coordinates and function paths are not shipped in `main.js`, so browser traffic stays behind the Vercel proxy, origin checks, metadata-only logging, and durable rate-limit hook.
- `api/chat.js` is the active same-origin Vercel governance proxy for homepage Brain chat submissions.
- `POST /api/chat` rejects untrusted browser origins, malformed messages, and malformed conversation IDs before forwarding prompt text to the Supabase Edge Function.
- Chat origin validation no longer trusts reconstructed `Host` / `X-Forwarded-Proto` origins. Missing-origin requests with unreviewed host headers are rejected, localhost origins are development-only unless explicitly configured in `CHAT_ALLOWED_ORIGINS`, and subscribe-only allowed origins no longer grant chat proxy access.
- `POST /api/chat` now rejects control-character message payloads before rate limiting, grounding checks, or upstream forwarding.
- When `RATE_LIMIT_REST_URL`, `RATE_LIMIT_REST_TOKEN`, and `RATE_LIMIT_SALT` are configured, `POST /api/chat` performs a durable pre-forward rate-limit check using a salted hash key, not the raw client address or prompt text.
- Durable chat rate-limit identity now shares the same explicit proxy-trust behavior as subscribe: forwarded client headers are ignored unless `RATE_LIMIT_TRUST_PROXY_HEADERS=true`, malformed forwarded values are discarded, and `User-Agent` rotation does not split the primary quota bucket.
- In `VERCEL_ENV=production`, `POST /api/chat` fails closed with a generic `503` when durable rate-limit configuration is missing, the limiter is unavailable, or the configured limiter returns a non-2xx HTTP error, preventing chat forwarding from bypassing the abuse gate.
- In `VERCEL_ENV=production`, `POST /api/chat` now requires explicit `SUPABASE_URL` and `SUPABASE_ANON_KEY` configuration before upstream forwarding. Missing upstream env returns a generic `503` after the durable limiter check and before any Supabase request.
- `POST /api/chat` now applies a server-side grounding gate for financial/Web3/live-deployment/autonomous-authority/wallet-signing, transaction-action, and selected future-vision/site-claim availability prompts before upstream chat forwarding. High-risk, future-vision, or qualification-required matches return `409` with `answer_generation: disabled`; risky-domain prompts without reviewed grounding also fail closed instead of requesting a bare upstream answer.
- `POST /api/chat` now derives grounding queries from overlapping public claim-registry tokens before forwarding, so prompts about reviewed claims such as the homepage "3 Live Blockchains" stat return claim-qualified grounding packets instead of ungrounded risky-domain blocks or bare upstream answers.
- Chat grounding `409` responses now embed the same public grounding packet schema used by `/api/grounding`, including `schema_version`, `mode`, `answer_policy`, citations, source citation indexes, required claim qualifications, refusal rules, review flags, and safety boundaries.
- Chat grounding `409` responses now include a top-level `display` contract (`grounding_review_required` / `citation_review_card`) so frontend and agent clients can render citations, claim qualifications, and refusal rules without treating the packet as an answer.
- Browser chat now treats `/api/chat` responses as terminal and no longer ships a direct provider fallback path, so grounding `409` and validation responses cannot bypass same-origin policy handling from the browser.
- Browser chat now renders `409` grounding-review packets from the backend `display` contract with citations, claim qualifications, and refusal rules, using `textContent` DOM nodes and no freeform reply rendering for non-OK responses.
- Public chat failures now return generic proxy errors instead of provider details; `CHAT_LOG_EVENTS=true` enables metadata-only chat logs without logging prompt text, anon keys, or upstream response bodies.
- The browser now calls only same-origin `/api/chat`; direct provider chat fallback coordinates and function paths are not shipped in `main.js`.
- `public/robots.txt`, `public/sitemap.xml`, and `public/llms.txt` are active Vite public metadata files for live routes only. `robots.txt` points crawlers at the sitemap and asks crawlers to avoid `/api/` paths.
- `public/ai-services.json` is the active public AI-service metadata file. It lists only current public API surfaces (`/api/architecture`, `/api/assets`, `/api/chat`, `/api/claims`, `/api/grounding`, `/api/organisms`, `/api/search`, `/api/status`, `/api/subscribe`, `/api/transmissions`) with bounded claim scopes, public citations, source files, source routes, and safety boundaries.
- `public/ai-services.json` now keeps creator-only social packet paths out of service discovery; asset-registry metadata cites public routes plus controller/registry/test files rather than caption/carousel handoff files.
- `public/data/architecture.json` is the active public architecture registry. It records source-backed concepts for cognitive layers, memory, approval governance, cell governance, public proof, and claim references without credentials or private procedures.
- `api/architecture.js` exposes the architecture registry as a read-only Vercel API with list, single-concept lookup, and category filtering.
- `/api/architecture` now returns stable generic JSON failures with `Cache-Control: no-store` for malformed lookup values, unknown ids, write methods, and registry-loader failures, avoiding public caching of error states and preventing filesystem/parser details from reaching clients.
- `/api/architecture` now validates `id` and `category` filters before registry loading, routes valid `GET` and `HEAD` requests through the shared durable rate-limit hook, sends salted route/client hashes only, and fails closed in production when limiter configuration is missing or unavailable.
- `public/data/assets.json` is the active public asset provenance registry. It records prepared asset packages with source file hashes, generated file hashes, byte counts, alt text, rights notes, publication status, manual approval gates, machine-readable authority boundaries, and public-safe approval record schemas.
- Asset packages now expose a canonical `asset_package_sha256` derived from package identity, public display metadata, source-file lists, source file hashes, generated file hashes, rights, approval gates, the full public approval schema, review/publication state, confidence, and authority boundaries so future approval records and provenance attestations have a concrete digest target.
- `/api/assets` now recomputes the canonical `asset_package_sha256` from package identity, public display metadata, complete source/generated file provenance, rights, approval gates, the full public approval schema, review/publication state, confidence, and authority boundaries, failing closed before public response if the stored digest is syntactically valid but noncanonical or based on incomplete inputs.
- `api/_shared/public-search.js` now runs the shared asset registry validator before search or grounding indexing, so `/api/search` and `/api/grounding` cannot bypass canonical digest, approval schema, and authority-boundary checks when reading `public/data/assets.json`.
- Asset registry tests now verify every listed source-file and generated-file byte count and SHA-256 across all packages; Transmission 28's memory control plane carousel provenance was refreshed to match the current local package files.
- The asset approval record schema defines required public fields for future reviewed publication/provenance approvals while keeping `approval_records` empty until a real reviewed approval exists.
- `api/assets.js` exposes the asset registry as a read-only Vercel API with list, single-package lookup, review-status filtering, publication-status filtering, validator-aware public cache headers, and write-method rejection.
- `/api/assets` successful public registry reads now use `Cache-Control: public, max-age=60, must-revalidate`, `ETag`, `Last-Modified`, and full-registry conditional `304` responses so approval-bearing asset metadata has a shorter freshness window and concrete cache validators.
- `/api/assets` now returns stable generic JSON failures with `Cache-Control: no-store` for malformed lookup values, unknown ids, write methods, and registry-loader failures, avoiding public caching of error states and preventing filesystem/parser details from reaching clients.
- `/api/assets` now uses the shared durable rate-limit hook before registry and metadata loading for valid asset registry reads. Limiter payloads contain salted route/client hashes only, `429` and limiter-unavailable responses are generic and `no-store`, and production fails closed when limiter configuration is missing or unavailable.
- `/api/assets` now fails closed with a generic non-cacheable error if future approval records reference a different asset package id or `asset_package_sha256`.
- `/api/assets` now fails closed with a generic non-cacheable error if asset authority boundaries grant high-risk posting, wallet, paid-media, on-chain, deployment, answer-generation, memory/file mutation, outreach, lead-import, or external-sync authority.
- `/api/assets` now validates authority-boundary `allowed_uses` and `prohibited_uses` against code-owned public-safe vocabularies, failing closed when allowed uses grant prohibited actions or required prohibitions are missing.
- `/api/assets` now validates source-file hash coverage, failing closed when listed source files and source hash paths diverge or contain duplicate path records.
- `/api/assets` now fails closed with a generic non-cacheable error if future approval records or schemas drift from the code-owned public allowlist: schema version/storage-boundary drift, prohibited schema fields, extra/private record fields, missing required public fields, unsupported approval scopes, invalid role/timestamp values, or unverified source/generated file hashes all block the public response.
- `/api/assets` now returns unknown-id errors before attaching public `ETag`/`Last-Modified` validators, and `HEAD /api/assets?id=<missing>` returns an empty non-cacheable `404` instead of a cacheable `200`.
- `public/data/claims.json` is the active public claim/evidence registry. It labels homepage and financial/Web3 claims by claim status, evidence status, risk level, source files, public citations, and interpretation boundaries.
- `api/claims.js` exposes the claim registry as a read-only Vercel API with list, single-claim lookup, claim-status filtering, evidence-status filtering, category filtering, public cache headers, and write-method rejection.
- `/api/claims` now returns stable generic JSON failures with `Cache-Control: no-store` for malformed lookup values, unknown ids, write methods, and registry-loader failures, avoiding public caching of error states and preventing filesystem/parser details from reaching clients.
- `/api/claims` now uses the shared durable rate-limit hook before registry loading for valid claim registry reads. Limiter payloads contain salted route/client hashes only, `429` and limiter-unavailable responses are generic and `no-store`, and production fails closed when limiter configuration is missing or unavailable.
- `public/data/organisms.json` is the active public organism registry. It records capabilities, safety boundaries, memory model labels, live route references, source files, claim references, and public review status without credentials or runtime-only evidence.
- `api/organisms.js` exposes the registry as a read-only Vercel API with public cache headers, single-organism lookup by `id`, and write-method rejection.
- `/api/organisms` now returns stable generic JSON failures with `Cache-Control: no-store` for malformed lookup values, unknown ids, write methods, and registry-loader failures, avoiding public caching of error states and preventing filesystem/parser details from reaching clients.
- `/api/organisms` now uses the shared durable rate-limit hook before registry loading for valid public organism reads. Limiter payloads contain salted route/client hashes only, `429` and limiter-unavailable responses are generic and `no-store`, and production fails closed when limiter configuration is missing or unavailable.
- `public/data/transmissions.json` is the active public transmission registry. It covers every active published transmission with topic tags, memory layers, citations, confidence, source files, and public review status.
- `api/transmissions.js` exposes the transmission registry as a read-only Vercel API with list, single-transmission lookup, and topic filtering.
- `api/_shared/claim-context.js` is the canonical public claim-context helper for transmission enrichment. It owns inferred transmission claim references, deterministic matched-claim sorting, public claim qualifications, and sensitive-topic review flags for `web3-safety`, `financial-safety`, and `approval-gates`.
- Direct `/api/transmissions` responses now decorate public transmission entries with inferred `claim_references`, compact `claim_context`, and public-safe `claim_qualifications` from the shared claim-context helper when source routes/files overlap, so high-risk financial/Web3 entries carry qualification labels, interpretation boundaries, and citations before they are used as RAG seed metadata.
- Direct `/api/transmissions` claim context also marks sensitive `web3-safety`, `financial-safety`, and `approval-gates` topic tags with `requires_qualification: true` and a `sensitive_topic_requires_grounding` review flag even when no explicit claim row matches, preventing Web3/approval seed content from looking low-risk by omission.
- `/api/transmissions` now returns stable generic JSON failures with `Cache-Control: no-store` for malformed lookup values, unknown ids, write methods, and registry/claim-loader failures, avoiding public caching of error states and preventing filesystem/parser details from reaching clients.
- `/api/transmissions` now uses the shared durable rate-limit hook before registry and claim-context loading for valid public transmission reads. Limiter payloads contain salted route/client hashes only, `429` and limiter-unavailable responses are generic and `no-store`, and production fails closed when limiter configuration is missing or unavailable.
- `api/status.js` exposes a read-only public backend status endpoint built from public AI-service metadata and public architecture, asset, claim, organism, and transmission registries.
- `GET /api/status` returns public-safe service ids, endpoints, asset/architecture/claim/organism/transmission registry counts, review status, and explicit safety boundaries; it does not report env values, provider keys, user submissions, runtime evidence, private prompts, owner manifests, public posting authority, or deployment details.
- `GET /api/status` now exposes the latest reviewed public transmission pointer from `public/data/transmissions.json` by highest `transmission_number`, limited to id, number, title, route, source file, and review status.
- `GET /api/status` now exposes public transmission numbering metadata (`latest_number`, `published_count`, `has_gaps`, and `missing_numbers`) so agents do not confuse published count with the latest transmission number when a skipped number is intentional.
- `GET /api/status` now aggregates public operational controls from `public/ai-services.json`, including durable-rate-limited endpoints, production-fail-closed endpoints, answer-generation-disabled endpoints, grounding-review-required endpoints, same-origin guarded endpoints, and limiter payload boundaries without exposing live env configuration.
- `GET /api/status` now validates the public asset registry through the shared asset provenance validator before reporting asset counts, so status fails closed instead of reporting healthy aggregate metadata when asset digests, approval schemas, or authority boundaries are invalid.
- `HEAD /api/status` now validates the same public status metadata and asset provenance gates as `GET` before returning a bodyless cacheable success; loader or validation failures return bodyless `no-store` errors.
- `/api/status` now keeps success responses public-cacheable but returns `Cache-Control: no-store` on method and registry-loader failures. Its in-process status cache refreshes when public metadata source file signatures change.
- `scripts/check-public-parity.mjs` now provides local and optional live public-proof parity checks over `/api/status`, `/api/search`, `/api/grounding`, sitemap, `llms.txt`, `ai-services.json`, archive, and homepage discovery surfaces. Local mode avoids live deployment claims; live mode is read-only production verification.
- Live public parity checks now convert network fetch failures into structured `live_fetch_reachable=false` reports with safe error codes and no stack traces, so production freshness audits fail closed instead of crashing on transient DNS/TLS/connectivity issues.
- Public parity now exercises `/api/search` and `/api/grounding` using a fixed public-safe smoke query, requiring disabled answer generation, citations or sources, and grounding-required policy before advertised RAG routes count as proven.
- Live public parity now classifies `/api/search` and `/api/grounding` `429`/`503` responses as endpoint-specific `search_rag_contract_supported` or `grounding_rag_contract_supported` failures with safe `HTTP_<status>` codes instead of generic reachability handling.
- Open parity gap: public parity does not yet exercise a reviewed `/api/chat` grounding-gate request.
- 2026-06-30 live parity audit: local public parity passes for Transmission 30, but `npm run check:public-parity:live` reports `live_status_contract_supported=false` because production `https://www.unwindcode.ai/api/status` does not yet expose the newer `status.registries.transmissions` contract. Treat this as live deployment drift, not local registry failure.
- `api/search.js` exposes a read-only public registry search endpoint over architecture, asset, claim, organism, and transmission metadata.
- `GET /api/search` requires a bounded query or reviewed filter, returns citations/source files/review status/claim references, supports `type`, `claim_status`, `evidence_status`, and `risk_level` filters, and rejects write methods without reading private filesystem content.
- `HEAD /api/search` now follows the same query validation, durable limiter, public registry loading, and asset provenance validation path as `GET` before returning a bodyless success; validation, limiter, or loader failures stay bodyless and `no-store`.
- Search results now include deterministic lexical ranking, match scores, matched terms, compact public-safe snippets, and claim-context summaries so downstream RAG/chat layers can cite and qualify results before answer synthesis.
- Search results now preserve public `memory_layers` and `memory_context` for architecture, organism, and transmission entries, and support a bounded `memory_layer` filter without adding embeddings or answer synthesis.
- `/api/search` now returns `answer_generation: disabled`, `synthesis_requires_grounding: true`, and a grounding-required answer policy so retrieval snippets are not treated as generated answers.
- Each `/api/search` result now carries per-result retrieval semantics (`answer_generation: disabled`, `snippet_policy: not_an_answer`, `retrieval_semantics: evidence_excerpt_requires_grounding`) so snippets remain evidence excerpts even when consumed outside the response envelope.
- Asset search results now include machine-readable approval context, `public_safe`, `requires_human_review`, and review flags for manual-approval-required, unposted, or non-public-safe asset packages, preventing prepared asset packages from looking like ordinary RAG evidence.
- Asset search results now preserve `authority_boundary` metadata so direct retrieval consumers can see that provenance records grant no automated posting, wallet, paid-media, on-chain publication, or unreviewed RAG authority.
- Asset search results now use a minimized retrieval projection for asset packages: internal creator packet paths are removed from `source_files`, citations fall back to the public asset registry when no public source file remains, raw `approval_records` are omitted, and only `approval_record_count` plus the approval schema version are exposed.
- `/api/search` now marks successful query-bearing responses `Cache-Control: private, no-store` and all validation/method/registry-loader errors `Cache-Control: no-store`, preventing accidental private query text or transient registry failures from being stored in shared caches.
- `/api/search` now uses the shared durable rate-limit hook before registry loading for valid public retrieval requests. Limiter payloads contain salted route/client hashes only, `429` and limiter-unavailable responses are generic and `no-store`, and production fails closed when limiter configuration is missing or unavailable.
- `api/_shared/public-search.js` is the shared public-registry retrieval helper used by `/api/search`, `/api/grounding`, and risky-domain chat grounding.
- `api/_shared/public-search.js` now memoizes public registry JSON in process using file size/mtime signatures for invalidation and deep-freezes the cached object, reducing repeated filesystem/JSON parse work while preventing callers from mutating shared retrieval state.
- `api/_shared/public-search.js` now indexes explicit public claim-reference fields (`claim_id`, statuses, `risk_level`, and `purpose`) for organism, architecture, and transmission retrieval, and can surface matched architecture/organism claim-reference purpose text in snippets without indexing arbitrary nested objects.
- `api/_shared/public-search.js` now enriches transmission search entries with the shared transmission claim-context decorator, including inferred claim references, public claim qualifications, sensitive-topic tags, and review flags when claim source routes, source files, or citation routes match a transmission that does not carry explicit `claim_references`.
- `api/_shared/grounding-policy.js` is the shared packet/policy helper used by `/api/grounding` and risky-domain `/api/chat` blocks. It keeps answer synthesis disabled, carries refusal rules and answer policy, and derives `requires_human_review` from the packet's review-required policy decision.
- `/api/search` and `/api/grounding` preserve financial/Web3 claim context for transmission-only retrieval, including Monad/SEC transmission searches and sensitive-topic transmissions without explicit claim rows, so high-risk claim labels and required review gates survive before answer synthesis.
- `api/grounding.js` exposes a read-only public grounding packet endpoint that returns top-five source snippets, citation indexes, required claim qualifications, refusal rules, and `answer_generation: disabled` before any RAG answer synthesis.
- Grounding packets now include frontend-ready `citation_display` metadata plus source-level `citation_display_refs`, keeping numbered public citation rendering explicit while preserving disabled answer synthesis.
- Each `/api/grounding` source now carries source-level answer safety semantics (`answer_generation: disabled`, `source_policy`, `retrieval_semantics`, and `answer_safety`) so public metadata safety is separated from standalone answer safety and packet-level review requirements survive when sources are consumed outside the packet envelope.
- Each `/api/grounding` source now preserves public `memory_layers` and defensively copied `memory_context` from the public registries while excluding private memory, runtime memory, user memory, and unstated embeddings.
- Asset grounding packets now preserve approval-required asset review flags and block synthesis with explicit asset approval/publication reasons when a source asset is not public-safe or requires manual approval.
- Asset grounding packets now inherit the minimized asset retrieval projection, keeping public route/registry citations plus safe alt text, approval context, approval-record count, publication state, and authority boundaries while excluding internal creator packet paths and raw approval records.
- Grounding packets now include machine-readable answer policy and review flags so downstream answer generation remains blocked until citations, claim qualifications, and refusal enforcement are explicitly reviewed.
- `/api/grounding` now marks successful query-bearing responses `Cache-Control: private, no-store` and all validation/method/registry-loader errors `Cache-Control: no-store`, preventing user query intent, grounding terms, and transient failures from entering shared caches.
- `/api/grounding` now uses the shared durable rate-limit hook before public grounding packet assembly. Limiter payloads contain salted route/client hashes only, `429` and limiter-unavailable responses are generic and `no-store`, and production fails closed when limiter configuration is missing or unavailable.
- `HEAD /api/grounding` now follows the same query validation, durable limiter, public registry loading, asset provenance validation, and grounding packet assembly path as `GET` before returning a bodyless success; validation, limiter, or loader failures stay bodyless and `no-store`.
- `vercel.json` now applies low-risk global browser security headers: `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy`, and `Strict-Transport-Security`. CSP remains intentionally deferred until external font/CDN/script usage is reviewed.

## Planning Doc Gaps

- `docs/REDESIGN_NOTES.md`, `docs/CONTENT_STRATEGY.md`, and `docs/FINALIZATION_QA.md` were not present in the active `unwindcode-website` package during this cycle.
- The active package now has `public/ai-services.json`; do not copy the local worktree variant because it references routes and service claims outside this package root.
- Do not blindly copy worktree metadata files into the active package: the worktree includes routes and Transmission 25 that are not live in this package root.

## Current Safety Boundary

- Keep `SUPABASE_SERVICE_ROLE_KEY` server-side only.
- Keep public responses generic on storage failures.
- Keep `SUBSCRIBE_ALLOWED_ORIGINS` limited to deployed UnwindCode.ai domains and reviewed preview hosts. Do not rely on request `Host` / `X-Forwarded-Proto` reconstruction as an implicit allowed origin.
- Keep subscribe logs metadata-only; do not log submitted email addresses, service role keys, or upstream response bodies.
- Keep subscribe success responses identifier-free; do not return submitted or normalized email addresses to public clients after storage.
- Keep subscribe rate-limit checks durable and privacy-preserving: send salted hashes and route/window metadata only, never submitted emails, raw IPs, service keys, or upstream bodies. In production, missing, unavailable, or HTTP-error limiter checks should fail closed before storage.
- Keep subscribe upstream configuration explicit in production: require `SUPABASE_URL` and at least one reviewed storage credential path (`SUPABASE_SERVICE_ROLE_KEY`/`SUPABASE_SERVICE_KEY` for REST or `SUPABASE_ANON_KEY`/`VITE_SUPABASE_ANON_KEY` for Edge Function fallback). Do not let production storage use baked-in upstream defaults.
- Keep `RATE_LIMIT_TRUST_PROXY_HEADERS=false` unless the deployment proxy has been reviewed to overwrite or strip client-supplied `cf-connecting-ip`, `x-real-ip`, and `x-forwarded-for` headers. If enabled, only valid IP-shaped proxy header values may influence the limiter key.
- Keep chat logs metadata-only; do not log submitted prompt text, anon keys, conversation payloads, or upstream response bodies.
- Keep `CHAT_ALLOWED_ORIGINS` separate from subscribe-only preview origin config unless chat/RAG access for that preview host has been explicitly reviewed.
- Keep chat rate-limit checks durable and privacy-preserving: send salted hashes and route/window metadata only, never raw prompts, raw IPs, anon keys, or upstream bodies. In production, missing, unavailable, or HTTP-error limiter checks should fail closed before forwarding.
- Keep chat upstream configuration explicit in production: require `SUPABASE_URL`/`VITE_SUPABASE_URL` and `SUPABASE_ANON_KEY`/`VITE_SUPABASE_ANON_KEY` before forwarding. Do not let production chat use baked-in upstream defaults.
- Keep `User-Agent` out of the primary durable limiter identity so clients cannot split quotas by rotating browser strings.
- Keep chat grounding gates server-side for financial/Web3/live-deployment/autonomous-authority/wallet-signing prompts, transaction-action prompts such as swaps/fund transfers/transaction signing, and claim-bearing future-vision availability prompts; do not request upstream answers for risky or qualification-required prompts until citations, claim qualifications, and refusal rules can be rendered. Preserve the shared grounding packet shape in `409` responses without echoing submitted prompts.
- Keep chat grounding display metadata declarative and non-executable. The `display` object may tell clients how to render citations, claim qualifications, and refusal rules, but it must not enable freeform answers, upstream fallback, wallet authority, posting authority, or synthesis.
- Keep browser submission flows behind same-origin serverless proxies only. Do not ship direct provider fallback coordinates, function paths, provider tokens, or baked-in upstream configuration in `main.js` or built browser assets.
- Keep `public/ai-services.json` limited to current public API endpoints, live source routes, public source files, bounded claim scopes, citations, and safety boundaries.
- Keep architecture registry entries source-backed to active public pages and transmissions, with claim references that match `public/data/claims.json`; do not include exact approval mechanics, deployment procedures, private notes, raw prompts, runtime evidence, or operational commands.
- Keep `/api/architecture` behind durable rate limiting before registry loading for valid public architecture requests, including `HEAD`. Limiter calls must use salted route/client hashes only and must not include concept ids, category filters, source files, registry contents, or raw client addresses.
- Keep `/api/architecture` error responses non-cacheable and generic; do not expose registry parser errors, filesystem paths, stack traces, or public-cache headers on `4xx`/`5xx` responses.
- Keep asset registry entries source-backed to reviewed asset packages; every listed source file must have exactly one matching source-file hash record, and generated-file hashes must carry byte counts, while alt text, rights notes, publication status, approval gates, authority boundaries, and public-safe approval record schemas must not grant automated public posting authority.
- Keep `asset_package_sha256` bound to complete public package metadata, complete source/generated provenance, rights, approval gates, the full public approval schema, review/publication state, confidence, and authority boundaries before future approvals or attestations reference the package.
- Keep `/api/assets` and the shared public search loader as enforcement points for canonical package digests; do not serve or index asset packages whose stored digest differs from the recomputed canonical payload.
- Keep asset approval records public-safe and role-based only. Do not include private approver identities, private notes, raw prompts, credentials, sensitive user data, wallet secret material, or sensitive on-chain data; leave `approval_records` empty unless a reviewed approval actually exists.
- Future asset approval records must match the package id and canonical package digest; mismatches should fail closed before public API responses or search retrieval.
- Future asset approval records must match the code-owned public allowlist exactly, use an allowed scope, carry valid public role/timestamp values, and set source/generated hash verification booleans to `true`; schema drift, private fields, missing required fields, unsupported scopes, invalid values, or unverified hash states should fail closed before public API responses or search retrieval.
- Keep asset authority boundaries fail-closed for high-risk authority booleans and free-form use labels; public provenance must not become automated posting, wallet signing, paid media, on-chain publication, deployment, answer-generation, mutation, outreach, lead-import, or external-sync authority by registry drift.
- Keep `/api/assets` success responses validator-aware with a short revalidation window while asset packages carry approval or publication state.
- Keep `/api/assets` behind durable rate limiting before registry loading for valid public asset requests. Limiter calls must use salted route/client hashes only and must not include asset ids, filters, source file paths, generated asset paths, registry contents, or raw client addresses.
- Keep `/api/assets` error responses non-cacheable and generic; do not expose registry parser errors, filesystem paths, stack traces, or public-cache headers on `4xx`/`5xx` responses.
- Keep claim registry entries source-backed to active public pages and transmissions; include claim/evidence status, risk labels, public citations, and interpretation boundaries before broad RAG answers. Do not turn high-risk Web3 or financial language into wallet authority, current deployment proof, or unqualified automation claims.
- Keep `/api/claims` behind durable rate limiting before registry loading for valid public claim requests. Limiter calls must use salted route/client hashes only and must not include claim ids, filters, claim labels, citations, source file paths, registry contents, or raw client addresses.
- Keep `/api/claims` error responses non-cacheable and generic; do not expose registry parser errors, filesystem paths, stack traces, or public-cache headers on `4xx`/`5xx` responses.
- Keep organism registry entries source-backed to active public files, with claim references that match `public/data/claims.json`; do not include private repositories, unreleased routes, runtime evidence, credentials, or hidden operational procedures.
- Keep `/api/organisms` behind durable rate limiting before registry loading for valid public organism requests. Limiter calls must use salted route/client hashes only and must not include organism ids, capability text, memory labels, claim references, source routes, registry contents, or raw client addresses.
- Keep `/api/organisms` error responses non-cacheable and generic; do not expose registry parser errors, filesystem paths, stack traces, or public-cache headers on `4xx`/`5xx` responses.
- Keep transmission registry entries aligned to active published transmission files only; do not include drafts, worktree-only posts, private notes, or unpublished routes.
- Keep transmission claim context centralized through `api/_shared/claim-context.js`; direct `/api/transmissions`, `/api/search`, and `/api/grounding` should not rebuild conflicting sensitive-topic or inferred-claim logic in separate endpoint files.
- Treat direct `/api/transmissions` output as decorated retrieval seed metadata, not generated answers. High-risk financial/Web3 entries include inferred claim references, claim context, claim qualification citations, and conservative sensitive-topic review flags, but downstream answer synthesis should still use `/api/grounding` so refusal rules, answer policy, source indexes, and human-review gates are preserved.
- Keep `/api/transmissions` behind durable rate limiting before registry and claim-context loading for valid public transmission requests. Limiter calls must use salted route/client hashes only and must not include transmission ids, topic filters, source routes, claim context, registry contents, or raw client addresses.
- Keep `/api/transmissions` error responses non-cacheable and generic; do not expose registry parser errors, filesystem paths, stack traces, or public-cache headers on `4xx`/`5xx` responses.
- Keep backend status output limited to validated public metadata files and aggregate counts; do not include secret configured state, provider responses, user submissions, runtime evidence, private prompts, owner manifests, deployment details, or raw generated asset payloads.
- Treat backend status latest-transmission metadata as a local public-registry pointer only, not live production deployment proof.
- Treat transmission numbering gaps as descriptive public registry metadata, not proof of missing deployment, hidden content, or publication failure by themselves.
- Keep backend status operational controls derived from public metadata only. It may list endpoints and public safety booleans, including grounding-review requirements, but must not report configured tokens, salts, env presence, provider health, raw limiter keys, client addresses, query text, prompts, or live deployment proof.
- Keep `/api/status` error responses non-cacheable and generic; do not expose registry parser errors, filesystem paths, stack traces, public-cache headers, or stale cached status after public metadata files change.
- Keep local parity checks scoped to local public files plus read-only status/search/grounding handler output. They may catch source-surface mismatch and public RAG contract drift, but they do not prove Vercel alias health or live production freshness. Use the explicit live parity mode only when network production verification is intended.
- Keep live parity network failures structured and non-secret: report reachability, public base URL, and safe machine-readable error codes only; do not print fetch stack traces, headers, credentials, request internals, or provider diagnostics.
- Keep live parity RAG endpoint failures structured and endpoint-specific. `/api/search` and `/api/grounding` `429`/`503` states should fail their own RAG contract checks with safe `HTTP_<status>` codes, not replace the whole report with a generic reachability failure.
- Keep parity RAG smoke queries fixed and public-safe; do not accept arbitrary prompts, private notes, credentials, raw user queries, or POST bodies in parity checks.
- Keep public search output limited to reviewed public registry fields; do not use it as private filesystem search, private prompt search, runtime evidence retrieval, deployment introspection, or source-write authority.
- Keep `/api/search` memory-aware but lexical: `memory_layer` filtering and public memory labels are allowed, but private memory, runtime memory, user memory, vector embeddings, and semantic-answer claims remain excluded until explicitly reviewed.
- Keep `/api/search` query-bearing responses private and non-cacheable; public registry content can be cached through static JSON files and read-only registry endpoints, but raw user query text should not become a shared-cache artifact.
- Keep `/api/search` behind durable rate limiting before registry loading for valid retrieval requests, including `HEAD`. Limiter calls must use salted route/client hashes only and must not include raw query text, raw client addresses, source snippets, or registry results.
- Keep public-registry memoization scoped to public JSON only; cached registries must remain immutable, file-signature invalidated, and resettable in tests, and runtime/private evidence must not be added to the memoized search loader.
- Keep high-risk Web3 and financial search results attached to claim status, evidence status, risk labels, citations, matching claim references, or sensitive-topic review flags before any downstream answer generation.
- Keep public claim-reference indexing allowlisted to explicit public fields only; do not deep-index arbitrary nested registry objects or hidden operational notes.
- Keep approval-required asset search results attached to approval context, `public_safe: false`, `requires_human_review: true`, and asset review flags before any downstream grounding or answer generation.
- Keep `/api/search` and `/api/grounding` asset retrieval projections minimized and validator-backed: exclude internal creator packet paths, raw approval records, and full approval schemas; expose only safe review/authority summaries, approval-record counts, schema version, and public route/registry citations. The detailed public provenance surface remains `/api/assets`.
- Keep asset authority boundaries attached to direct asset registry and search results; provenance hashes are not permission to post, sign, pay, publish on-chain, or use unreviewed assets as answer evidence.
- Treat search ranking/snippets as retrieval scaffolding only; they are not generated answers, live provider health, proof of deployment, or permission to execute financial/public actions.
- Keep `/api/search` answer generation disabled. Downstream synthesis must request `/api/grounding`, render citations, and review claim qualifications before any answer endpoint is added.
- Keep grounding packets as citation scaffolding only; do not let `/api/grounding` generate prose answers, claim live provider health, infer deployment state, use runtime evidence, or relax Web3/financial refusal rules.
- Keep `citation_display` as a frontend rendering contract only; it does not grant synthesis, execution authority, wallet authority, or current deployment proof.
- Keep grounding memory fields public-only and defensively copied; consumers mutating `memory_layers` must not mutate `memory_context.layers`.
- Keep grounding source `answer_safety.public_metadata_safe` separate from `answer_safety.answer_safe`; even public-safe sources require citation rendering, claim-qualification review, and packet-level answer policy review before synthesis, with `human_review_required` inheriting packet-level review decisions.
- Keep `/api/grounding` query-bearing responses private and non-cacheable; grounding packets echo query context and should not preserve accidental private or sensitive questions in shared caches.
- Keep `/api/grounding` behind durable rate limiting before packet assembly for valid retrieval requests, including `HEAD`. Limiter calls must use salted route/client hashes only and must not include raw query text, citations, snippets, qualifications, or packet contents.
- Treat grounding `answer_policy` as the enforceable contract for future RAG endpoints: synthesis stays disabled until citations, claim qualifications, refusal rules, sensitive-topic review flags, and answer endpoint behavior are reviewed. Packets with missing citations or review-required policy decisions must require human review.
- Keep deployment headers conservative and compatible: preserve no-sniff, frame denial, strict-origin referrers, disabled sensitive browser permissions, and HSTS. Add CSP only after script/font/CDN requirements are mapped and tested.
- Treat homepage live/on-chain/real-market language through `public/data/claims.json` labels before further amplifying metadata.
- Do not place private prompts, raw sensitive notes, or secrets in public static files.
