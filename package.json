{
  "name": "unwindcode-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:public-parity": "node scripts/check-public-parity.mjs",
    "check:public-parity:live": "node scripts/check-public-parity.mjs --live",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
